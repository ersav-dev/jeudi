# AUDIT COMPLET — jeudi. (2026-07-02)

> Relecture intégrale de l'app par 4 passes parallèles : cœur (App.tsx + écrans), backend/sécu (db.ts + Supabase + RLS), carte & moteurs, PWA/infra.
> Référence pour les prochaines sessions. Cocher au fur et à mesure.

---

## 🔴 CRITIQUE — à corriger AVANT d'inviter qui que ce soit

### Sécurité / données
- [ ] **RLS `relations` falsifiable** (0001_init.sql:227-230) : la policy INSERT ne contraint pas `statut` → n'importe quel inscrit peut s'insérer `statut='accepte'` dans le cercle de n'importe qui et lire tous ses spots « cercle » (+ tips + photos via `lieu_visible`). La policy UPDATE laisse aussi le demandeur s'auto-accepter. Fix : `statut='demande'` forcé à l'INSERT, passage à `accepte` réservé à `vers_id`.
- [ ] **Bucket Storage `photos` PUBLIC** (0002_storage_photos.sql) : URLs stables/devinables (`<uid>/profil.jpg` !) → photos de lieux privés + portrait accessibles SANS compte. Le commentaire du fichier (« filtré par la table ») est faux : la RLS protège la table, pas le binaire. Fix : bucket privé + `createSignedUrl`.
- [ ] **`changerPhotoProfil` efface bio/insta/naissance** (App.tsx:627-638 + db.ts sauverProfil) : l'appel partiel écrase le profil complet, local ET cloud. Fix : `sauverProfil` en merge (lit-fusionne-écrit).
- [ ] **Identité d'Ersan codée en dur** (App.tsx:597/607/634, Onboarding.tsx:38/65) : `prenom:'Ersan'`, `naissance:'1991-03-06'` → tout nouvel utilisateur devient Ersan, poussé dans Supabase.
- [ ] **Collision d'IDs seed ↔ cloud** (db.ts:355-368, seed.ts:334) : les ~30 spots seed `proprietaire='moi'` ont des ids non-UUID (`id-<timestamp>-…`) ; `majLieu` vers le cloud échoue en silence (loggé + repli local) → l'édition semble marcher mais ne sync jamais.

### PWA / carte
- [ ] **Aucun service worker** : app installée = page blanche hors-ligne (métro, sous-sol de bar). Fatal pour une app de sorties. Fix : `vite-plugin-pwa` (autoUpdate + toast « nouvelle version »).
- [ ] **`apple-touch-icon` → 404** (index.html:19 pointe `/icon-192.png` inexistant) : icône d'écran d'accueil iPhone = screenshot moche. Manifest sans PNG 192/512 ni maskable.
- [ ] **750 markers DOM sans clustering** (Carte.tsx:291) + markers détruits/recréés dès que `vus` change, avec `fitBounds` relancé (Carte.tsx:240-319) → la carte recadre l'utilisateur + rame par construction. Fix : source GeoJSON + layer symbol `cluster:true`.
- [ ] **`majLabels` = layout thrashing** (Carte.tsx:176-216) : ~750 `getBoundingClientRect` par frame de pan/zoom.

### Règle produit
- [ ] **`★ super pote`** (App.tsx:1596) : une ÉTOILE dans le chrome. Double violation (étoiles bannies + icônes encre only).
- [ ] **Émoji `⚽`** dans la page « pratique » du bottom-sheet (Carte.tsx:468) — `IBallon` existe déjà dans icones.tsx, l'utiliser.

---

## 🟠 MOYEN — bugs réels, à traiter par lots

### Perte / corruption de données
- [ ] Validation : ajouter une photo écrase TOUTES les anciennes de la même catégorie (App.tsx:1921).
- [ ] `archiverLieu`/`supprimerLieu` : RLS bloque → `error=null`, 0 ligne affectée = considéré « succès », pas de repli local (db.ts:393-411). Vérifier le count + `.eq('owner_id', monId)`.
- [ ] Miroir offline : lecture cloud partielle (plafond 1000 lignes / race 2 appareils) purge des spots du cache (db.ts:331-338).
- [ ] Spot créé hors-ligne avec session valide = `throw` → ni cloud ni local, PERDU (db.ts:360). Il faut une write-queue offline.
- [ ] Photos orphelines : `syncPhotosLieu`/`supprimerLieu` ne suppriment jamais les blobs Storage (db.ts:270-288).
- [ ] Vider son tip ne l'efface jamais (App.tsx:1920).
- [ ] `effacer()` : `deleteDatabase` non attendu avant `reload()` (App.tsx:372-375).

### RGPD
- [ ] `effacerTout()` = local only. Aucune suppression de compte, aucun export. Les données cloud survivent (db.ts:1048-1053).
- [ ] Policy `profils lisibles using(true)` : DATE DE NAISSANCE exacte de tous exposée à tout inscrit (0001:157-158). Exposer l'âge calculé, pas la date.

### Sync manquante (perdu au changement de téléphone)
- [ ] `recos` : édité par l'UI mais ABSENT du SQL et du mapping → jeté à chaque sync.
- [ ] `tips` : table SQL jamais lue/écrite (`from('tips')` = 0 occurrence).
- [ ] `sorties` : table SQL prête, mais 100 % localStorage.
- [ ] `relations`/cercle/super-potes : localStorage only → côté cloud, personne n'est dans le cercle de personne.
- [ ] favoris · comparer · vus · suivis · signalés · bofs · tagline · météo : localStorage only.

### État / UX
- [ ] Super potes : 2 sources de vérité (`MEMBRES.m.proche` seed vs `lesProches()`) → 2 compteurs différents sur le MÊME écran profil ; toggler ne change ni stat ni filtre (App.tsx:796/1321 vs 537/1449).
- [ ] Swipe droite deck = sortie irréversible, « jeter » au récap ne la retire pas → « alors, X ? » pour des lieux jamais faits (CeSoir.tsx:417/397).
- [ ] Demandes d'amis : « accepter » et « ignorer » font EXACTEMENT la même chose (App.tsx:585-589/944-948).
- [ ] Splash : timer 3,8 s réinitialisé à chaque re-render de App (`onFini` inline) (Splash.tsx:5-8).
- [ ] Notifs → `setAttente([s])` sans `attenteTotal` → compteur « 3/3 » faux (App.tsx:965/1694).
- [ ] `authPret` false après splash → `return null` = écran noir (App.tsx:869).
- [ ] « jeudi · 22h » choisi UN JEUDI → renvoie au jeudi suivant (CeSoir.tsx:100).
- [ ] Drags sans `onPointerCancel` (CeSoir, Onboarding) → carte figée décalée.
- [ ] Visionneuse : drag souris = change de photo ET ferme ; ✕ sans handler propre (App.tsx:2454-2470).
- [ ] Double-tap « c'est dit. » = lieu en double ; fallback lat/lng 0,0 = spots à Null Island (App.tsx:3175/3024).
- [ ] Marker « moi » posé une fois : géoloc tardive → pin à Vendôme mais distances recalculées ailleurs (Carte.tsx:125-128).
- [ ] Sélection carte : classes actif/grisé perdues quand les pins sont recréés (Carte.tsx:323-352).

### Moteurs (labo)
- [ ] « trop loin » du groupe mesuré depuis MA position, pas depuis le barycentre triangulé (groupe.ts:155) — contredit « à mi-chemin ».
- [ ] Lieu SANS envie taggée = satisfait tout le monde (crédit plein) ; idem budget inconnu → les lieux sans données battent les lieux renseignés (groupe.ts:52-55/99-101). Passer en demi-crédit.
- [ ] `verdictDeGroupe`, `pondererParProches`, `classerAutour` : écrits mais JAMAIS branchés à l'UI.
- [ ] Chip « ouvert » de la recherche garde les horaires inconnus (recherche.ts:138).
- [ ] Désélectionner toutes les envies groupe → retombe sur `['apéro']` en silence (EcranGroupe.tsx:58).
- [ ] `signalProche` matche par prénom (string) → homonyme hors cercle = faux « porté par ton cercle ». À passer en ids stables avec Supabase (deck.ts:21-27).
- [ ] Adresse introuvable / Nominatim en erreur → RIEN ne se passe (EcranRecherche.tsx:58-64).
- [ ] PickerCouleur : luminosité capturée au 1er render, jamais resync sur clic Pantone + dérive HSV cumulative (PickerCouleur.tsx:66/142).
- [ ] « selon Karim » = données INVENTÉES (hash déterministe), rien dans l'UI ne le signale (regard.ts:44-52).

### Réseau
- [ ] Nominatim ×3 endroits (autour.ts, db.ts:717/957, App.tsx:2985) : pas de `r.ok`, pas d'AbortController, pas de throttle 1 req/s, pas de User-Agent → réponses périmées qui écrasent les fraîches + risque de ban IP. Fix : wrapper unique.
- [ ] Aucun timeout/retry sur Supabase : sur réseau mobile dégradé, `tousLesLieux` peut pendre >60 s.
- [ ] localStorage sans borne : `jeudi-adr-<id>` par lieu + `jeudi-bofs` qui croît à l'infini → quota ~5 Mo approché.

### Perf
- [ ] `baseCarte`/`lieuxFiltres`/`aNoter` recalculés à CHAQUE render de App sans useMemo → chaque frappe dans bio/tagline re-render 3 181 lignes + relance filtres/tris/lectures storage (App.tsx:816-866).
- [ ] Recherche : pas de debounce + re-normalisation du « foin » de 750 lieux à chaque frappe (EcranRecherche.tsx:45, recherche.ts:112).
- [ ] Carrousel carte : 750 boutons DOM non virtualisés (Carte.tsx:497-541).
- [ ] Seed (~140 Ko de TS) importé statiquement dans le bundle principal alors qu'il ne sert qu'une fois → `import()` dynamique dans `importerSeed()` (seed.ts:2-4).
- [ ] `<img>` index sans `loading="lazy"` ni dimensions (LigneIndex.tsx:160/253).

### Qualité
- [ ] `strict: true` ABSENT de tsconfig.app.json (retiré du template) → null-checks désactivés.
- [ ] eslint rouge : 11 erreurs 3 warnings (dont `exhaustive-deps` EcranRecherche.tsx:45 = vrai bug). Aucun CI.
- [ ] ZÉRO test (db.ts + moteurs sans filet).
- [ ] `rooftops.ts` mort (18 Ko, importé nulle part) ; en-têtes « ISOLÉ » périmés dans groupe.ts/recherche.ts.
- [ ] App.tsx = 3 181 lignes, 7 composants, 40 états → extraire Fiche/Validation/FormAjout/Reglages/CarteCurateur + hooks useLieux/useComparer/useAppuiLong/useFeuilletagePhotos.
- [ ] Onglet profil entièrement en styles inline (App.tsx:1286-1567).

---

## 🟡 MANQUES structurels
- [ ] Balises Open Graph / Twitter (og:image 1200×630) → liens WhatsApp tout nus alors que c'est LE canal de croissance.
- [ ] Error tracking (Sentry/GlitchTip dans GardeFou) : crashs des testeurs iPhone invisibles.
- [ ] Analytics respectueux (Plausible/Umami) : zéro visibilité rétention.
- [ ] Mécanisme d'update visible (toast « nouvelle version — recharge ») ; package.json figé à 0.0.0.
- [ ] Polices Google non self-hostées (3 familles, bloquant + rien hors-ligne).
- [ ] Photos seed = loremflickr (souvent down, cassé hors-ligne).
- [ ] `public/textures/` 6,8 Mo + maquettes `papier*.html` déployées publiquement sur Vercel.
- [ ] Headers Vercel : pas de Cache-Control explicite, pas de CSP/X-Frame-Options.
- [ ] Onboarding : `max="2012-12-31"` autorise ~14 ans pour une app orientée bars.
- [ ] États vides : Grand Jeudi avec 0 spot public = grille vide après la promesse du voile.
- [ ] Tuto 1ʳᵉ visite (déjà noté dans REPRISE) toujours pas fait.
- [ ] A11y : `role="button"` sans tabIndex/clavier (5 endroits), cibles < 44 px (pastilles WC, ⋯, chevrons), mono 10-11 px à opacité 0.5 = contraste < 4.5:1.
- [ ] Glyphes texte `⌄ ✕ ⋯ ✓ ↺ ‹ ›` partout — pas des émojis, mais pas « encre SVG » au sens strict. À trancher.
- [ ] Le mot « avis » apparaît 2× (Splash, Onboarding) en usage auto-référentiel (« pas 4 000 avis google ») — à trancher aussi.
- [ ] Pastilles ●●○ utilisées pour les critères gradués des potes : validé en mémoire projet mais contredit « seule exception : WC » du concept — à graver noir sur blanc dans CONCEPT.md.

---

## 🔵 PLAN D'ATTAQUE PROPOSÉ (sessions courtes, une par bloc)

1. **Session SÉCU** (migration 0003) : RLS relations + bucket privé/URLs signées + policy profils sans naissance. Court, chirurgical, débloque l'invitation de vrais gens.
2. **Session PROFIL & IDENTITÉ** : sauverProfil en merge + retirer Ersan/1991 en dur + unifier super potes (une seule source).
3. **Session PWA** : vite-plugin-pwa + icônes PNG/maskable + apple-touch-icon + OG tags + self-host polices. Le meilleur ratio effort/impact.
4. **Session CARTE** : GeoJSON + cluster + diffing markers + sortir `vus` de l'effet + fitBounds une seule fois. La carte redevient fluide.
5. **Session DONNÉES** : write-queue offline + count vérifié sur archiver/supprimer + collision IDs seed + `recos` dans le schéma.
6. **Session POLISH DA** : ★ → icône encre, ⚽ → IBallon, glyphes → set icones.tsx, mot « avis », états vides.
7. **Session QUALITÉ** : strict:true + eslint vert + premiers tests (db.ts mapping + moteurs) + Sentry.
8. **RGPD** (avant ouverture publique) : suppression compte (Edge Function) + export JSON.
